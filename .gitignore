node_modules
.specs/scratchpad/

# macOS
.DS_Store

# Python virtualenv (local tooling)
.venv/

# Ad-hoc script output
out.json

# Generated knowledge graph (graphify). The graph itself is tracked so the
# repo carries its own map; the machine-specific and regenerable parts are not.
graphify-out/cache/
graphify-out/[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]/
graphify-out/.graphify_python
graphify-out/.graphify_root
